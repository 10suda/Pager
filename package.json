{
  "name": "pager-extension",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "engines": {
    "node": ">=22"
  },
  "scripts": {
    "build": "node scripts/build.mjs",
    "check": "npm test && npm run build && npm run verify",
    "test": "node --test",
    "verify": "node scripts/verify-build.mjs"
  },
  "dependencies": {
    "@supabase/supabase-js": "^2.116.0"
  },
  "devDependencies": {
    "dotenv": "^18.0.0",
    "esbuild": "^0.28.2"
  }
}
